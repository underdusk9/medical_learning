# 2026-06-21 工作日志

## 清理未使用文件 & section 统一

### 删除的文件
- `assets/questions/default.json` — 旧默认题库，已废弃
- `assets/questions/source/structure_final.txt` — 说明文件，未使用
- `now/` 整个目录 — 所有中间文件，数据已导入 source/

### section 字段统一
- `学成选择题_消化系统(1)_part02_normalized_global_unique.json` 中 4 题（id=1152-1155）的 section 从"消化系统疾病"修正为"诊断学基础"
- 所有 13 个 JSON 中同一 chapter 内 section 已全部一致，无混合情况

### 当前题库概况
- 28 个源文件，全部在 constants.dart sourceJsonPaths 中引用
- subject: 生理学(15) + 内科学(9) + 病理学(4) = 28 个 JSON
- **总计：1,560 题**

## 导入生理学题库（474 题）

### 改动
- `assets/questions/source/`：新增 15 个生理学 JSON 分片（ID 4001-5474，474 题）
- `docs/`：保存用户提供的科目章节标注框架
- `lib/core/constants.dart`：`sourceJsonPaths` 新增 15 条，DB version 升至 4
- `lib/core/database_helper.dart`：v3→v4 重置 seed 标志触发自动导入

### 当前题库
- 28 个源文件，3 个学科
- 生理学：474 题（15 文件）✅ 新入库
- 内科学：750 题（9 文件）
- 病理学：336 题（4 文件）
- **总计：1,560 题**

---

## 版本更新 v1.2.0

- `pubspec.yaml`：版本号 1.1.0 → **1.2.0**
- `CHANGELOG.md`：新增 v1.2.0 条目（生理学题库 + 清理 + 图标）
- `lib/screens/settings_screen.dart`：显示版本同步更新

## App 图标

- 使用 AI 生成冰蓝渐变风格图标，用户选定天蓝版
- `pubspec.yaml`：添加 `flutter_launcher_icons` 配置
- Android mipmap 全部 5 个分辨率已替换为新图标
- 清理 `assets/images/` 中 3 个废弃的生成图（省 ~3MB）

## 启动页改造

- `drawable/launch_background.xml`（普通版 + v21 版）：白底 + 居中显示 App 图标
- `lib/main.dart`：
  - 未填密码 → 纯白屏无转圈，与原生启动页无缝衔接
  - 填了密码 → 显示自定义图片 + 底部加载圈，无强制保底延迟
  - 去掉了之前加的 2 秒强制延迟

## App 更名

- `AndroidManifest.xml` `android:label` → **小han刷题**
- `lib/app.dart`、`lib/screens/home_screen.dart`、`lib/screens/settings_screen.dart` 统一改名
- 涵义：用户名字中的"涵" + 女朋友姓"韩"，双关拼音
