# 2026-06-16 工作日志

## 医学考研刷题 Flutter App — 全功能开发完成

### 工作流
- 标准 SOP（中大型需求）：产品经理 → 架构师 → 工程师 → QA
- 团队：software-med-quiz，4位成员协作

### 产出
- **PRD 文档**：`docs/prd.md`（7项P0 + 5项P1 + 3项P2需求）
- **架构设计文档**：`docs/architecture.md`（Layer-First结构，Riverpod+sqflite+GoRouter选型）
- **完整代码**：32个.dart源文件 + pubspec.yaml + 50题种子数据
- **QA验证**：2轮验证，修复4个Bug（2严重+2低优先级），最终PASS

### 关键技术决策
- 状态管理：flutter_riverpod（手写Provider，不用code generation）
- 数据库：sqflite（4表+3索引）
- 路由：go_router（8条路由）
- 多选答案格式：字符串"ABD"，isMultiple兼容"multiple"和"multi"
- Bookmark/Note id：int? 可空，插入时由SQLite AUTOINCREMENT

### Bug修复记录
1. BUG-1：Bookmark/Note id硬编码0 → 改int? + 条件toMap
2. BUG-2：QuestionDrawer index vs questionId映射 → 转换index-keyed Map
3. BUG-3：QuestionFilter hashCode不一致 → Object.hashAll
4. BUG-4：type兼容 → isMultiple同时检查"multiple"和"multi"

---

## 题库导入兼容性修复

### 新增题库（4个文件，来自 source/ 目录）
- 内科呼吸系统.json（~90题，id 1300-1389）
- 学成选择题_消化系统.json
- 学成选择题_泌尿系统.json（id 1173+）
- 病理学_选择题.json（id 1600+，含analysis字段）

### 兼容性修复（3个问题）
1. **多选答案格式**：用户JSON用逗号分隔"A,B,C"，App期望"ABC" → fromJson中自动去除逗号
2. **选项字母前缀**：用户JSON含"A. "前缀，App动态生成标签 → fromJson中自动剥离前缀
3. **超过26选项**：用户题目选项到a/b/c → question_card新增_getOptionLabel支持A-Z和a-z

### 修改文件
- lib/models/question.dart — fromJson增加答案标准化+选项前缀剥离
- lib/widgets/question_card.dart — _getOptionLabel支持>26选项
- lib/core/constants.dart — 新增sourceJsonPaths列表
- lib/services/seed_service.dart — 首次启动自动导入source/目录4个题库
- pubspec.yaml — assets新增assets/questions/source/目录
- assets/questions/source/ — 复制4个题库JSON文件

---

## 题库分片整理（每文件100题）

用户重新整理 source/ 中题库为10个分片文件：
- 内科呼吸系统: part01-03（237题）
- 泌尿系统: part01（94题）
- 消化系统: part01-02（188题）
- 病理学: part01-04（336题）
- 扩展题库合计: 855题（+50种子 = 905题）

更新 constants.dart 中 sourceJsonPaths 为10个新路径，替换 assets/questions/source/ 中旧文件。

---

## 答案订正功能

新增答题页答案订正功能，用户可在答题时修正错误答案：
- lib/dao/question_dao.dart — 新增 updateAnswer(id, newAnswer) 方法
- lib/providers/question_provider.dart — 新增 correctAnswer(ref, id, newAnswer) 函数
- lib/screens/quiz_screen.dart — AppBar 新增订正按钮(edit_note 图标)，弹出对话框输入新答案，多选自动排序规范化
